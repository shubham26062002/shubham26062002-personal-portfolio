type Color = 'sea-green' | 'coral-pink'

type SectionId = 'home' | 'about' | 'skills' | 'services' | 'portfolio' | 'contact'

type ContactFormFieldId = 'firstName' | 'lastName' | 'email' | 'subject' | 'message'

type ContactFormValues = {
    firstName: string,
    lastName: string,
    email: string,
    subject: string,
    message: string,
}